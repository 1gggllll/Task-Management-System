import { Typography } from 'antd';

const { Title } = Typography;

const TasksPage = () => {
  return (
    <div>
      <Title level={2}>任务管理</Title>
      <p>任务列表页面</p>
    </div>
  );
};

export default TasksPage;